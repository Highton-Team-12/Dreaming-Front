import { Route, Routes } from "react-router-dom";
const Router = () => {
  return (
    <Routes>
      <Route path="/sign" element={<div>asasdd</div>}></Route>
    </Routes>
  );
};

export default Router;
